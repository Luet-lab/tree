steps:
- emerge -b ={{.PackageCategory}}/{{.PackageName}}-${PACKAGE_VERSION}
requires:
- category: "{{.LayerCategory}}"
  version:  "{{.LayerVersion}}"
  name:     "{{.LayerName}}"
{{ if .Binhost }}
includes:
- /usr/portage/packages/.*
{{end}}